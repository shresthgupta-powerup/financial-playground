# Streamlit playground runtime. Engine lines mirror the handoff's
# requirements.txt (versions = the app backend's lower bounds, 2026-07-17).
streamlit>=1.60.0
pandas>=3.0.3
numpy>=2.5.1
python-dateutil>=2.9.0.post0
pydantic>=2.13.4          # only for code/app/planning/schemas.py
openpyxl>=3.1.5          # advisor Excel export
psycopg2-binary>=2.9.12   # import-time need of glide_paths_repo.py only (app-coupled, reference)
pytest>=9.1.1            # to run code/tests (the executable spec)
